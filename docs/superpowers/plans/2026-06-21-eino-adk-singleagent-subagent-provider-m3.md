# M3.2 SingleAgent-backed Subagent Provider Plan

## Checklist

- [x] Add tests for `subagent_refs` parsing, draft/version loading, name
      fallback, duplicate validation, and missing agent errors.
- [x] Implement `ADKSubagentReferenceProvider`,
      `ADKRunConfigSubagentReferenceProvider`, and
      `ADKSingleAgentSubagentDefinitionProvider`.
- [x] Keep full child ADK agent construction as the next M3 slice.
- [x] Update `AGENTS.md` and the DeerFlow parity roadmap.
- [x] Pass focused tests, full `application/agentthread` tests, and
      `git diff --check`.
